overwrite: true
schema: 'http://localhost:4000/graphql'
documents: 'src/graphql/*'
generates:
  src/lib/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
    config:
      strictScalars: true
      scalars:
        DateTime: string
