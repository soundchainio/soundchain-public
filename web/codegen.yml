overwrite: true
schema: "http://localhost:4000"
documents: "graphql/*"
generates:
  lib/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
