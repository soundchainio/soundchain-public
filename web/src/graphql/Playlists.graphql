query GetUserPlaylists($sort: SortPlaylistInput, $page: PageInput) {
  getUserPlaylists(sort: $sort, page: $page) {
    nodes {
      id
      title
      playlistTracks {
        trackEditionId
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
