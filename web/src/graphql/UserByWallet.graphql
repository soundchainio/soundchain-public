query UserByWallet($walletAddress: String!) {
  getUserByWallet(walletAddress: $walletAddress) {
    id
    profile {
      id
      displayName
      profilePicture
      userHandle
      followerCount
      followingCount
      verified
      teamMember
      badges
    }
  }
}
