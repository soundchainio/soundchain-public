mutation UploadTrack($input: UploadTrackInput!) {
  uploadTrack(input: $input) {
    track {
      id
      title
      file
      artworkUrl
      uploadUrl
      muxUpload {
        url
      }
    }
  }
}
