mutation CreatePlaylist($input: CreatePlaylistData!) {
  createPlaylist(input: $input) {
    playlist {
      id
      title
      description
      playlistTracks {
        playlistId
      }
    }
  }
}
