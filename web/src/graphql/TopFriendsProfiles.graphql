query TopFriendsProfiles($profileId: String!) {
  profile(id: $profileId) {
    id
    topFriendsProfiles {
      id
      displayName
      profilePicture
      userHandle
      verified
      teamMember
      isOnline
    }
  }
}
