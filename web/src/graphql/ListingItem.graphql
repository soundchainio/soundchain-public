query ListingItem($tokenId: Float!) {
  listingItem(tokenId: $tokenId) {
    listingItem {
      id
      owner
      nft
      tokenId
      quantity
      pricePerItem
      startingTime
    }
  }
}
