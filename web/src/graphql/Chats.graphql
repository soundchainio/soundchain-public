query Chats($page: PageInput) {
  chats(page: $page) {
    nodes {
      id
      profile {
        displayName
        profilePicture
      }
      message
      unread
      createdAt
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
