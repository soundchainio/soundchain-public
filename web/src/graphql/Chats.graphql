query Chats($page: PageInput) {
  chats(page: $page) {
    nodes {
      id
      profile {
        id
        displayName
        profilePicture
        verified
        teamMember
        userHandle
        badges
        isOnline
      }
      message
      unread
      createdAt
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
