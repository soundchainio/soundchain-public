query BidsWithINfo($auctionId: String!) {
  bidsWithInfo(auctionId: $auctionId) {
    bids {
      amount
      userId
      profileId
      createdAt
      user {
        handle
      }
      profile {
        profilePicture
        displayName
      }
    }
  }
}
