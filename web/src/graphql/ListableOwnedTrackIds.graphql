query ListableOwnedTrackIds($filter: FilterOwnedTracksInput!) {
    listableOwnedTracks(filter: $filter) {
        nodes {
            id
            nftData {
                tokenId
            }
        }
    }
}
