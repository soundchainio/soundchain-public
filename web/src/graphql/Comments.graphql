query Comments($postId: String!, $page: PageInput) {
  comments(postId: $postId, page: $page) {
    nodes {
      ...CommentComponentFields
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
