query Following($profileId: String!, $page: PageInput) {
  following(id: $profileId, page: $page) {
    nodes {
      id
      followedProfile {
        displayName
        profilePicture
      }
    }
    pageInfo {
      hasNextPage
      endCursor
      totalCount
    }
  }
}
