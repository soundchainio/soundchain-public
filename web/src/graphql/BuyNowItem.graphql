query BuyNowItem($tokenId: Float!) {
  buyNowItem(tokenId: $tokenId) {
    buyNowItem {
      id
      owner
      nft
      tokenId
      pricePerItem
      pricePerItemToShow
      OGUNPricePerItem
      OGUNPricePerItemToShow
      acceptsMATIC
      acceptsOGUN
      startingTime
    }
  }
}
