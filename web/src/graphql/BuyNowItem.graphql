query BuyNowItem($tokenId: Float!) {
  buyNowItem(tokenId: $tokenId) {
    buyNowItem {
      id
      owner
      nft
      tokenId
      contract
      pricePerItem
      pricePerItemToShow
      startingTime
    }
  }
}
