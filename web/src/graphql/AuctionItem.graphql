query AuctionItem($tokenId: Float!) {
  auctionItem(tokenId: $tokenId) {
    auctionItem {
      id
      owner
      nft
      tokenId
      contract
      startingTime
      endingTime
      reservePrice
      reservePriceToShow
    }
  }
}
