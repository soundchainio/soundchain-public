query AuctionItem($tokenId: Float!) {
  auctionItem(tokenId: $tokenId) {
    auctionItem {
      id
      owner
      nft
      tokenId
      startingTime
      endingTime
      reservePrice
      reservePriceToShow
    }
  }
}
