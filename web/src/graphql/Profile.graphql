query Profile($id: String!) {
  profile(id: $id) {
    id
    displayName
    profilePicture
    coverPicture
    socialMedias {
      facebook
      instagram
      soundcloud
      twitter
    }
    userHandle
    isFollowed
    isSubscriber
    followerCount
    followingCount
    musicianType
    bio
  }
}
