query Profile($id: String!) {
  profile(id: $id) {
    id
    displayName
    profilePicture
    coverPicture
    defaultCoverPicture
    socialMedias {
      facebook
      instagram
      soundcloud
      twitter
    }
    userHandle
    isFollowed
    isSubscriber
    followerCount
    followingCount
    musicianTypes
    bio
  }
}
