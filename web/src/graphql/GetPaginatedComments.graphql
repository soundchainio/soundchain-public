query GetPaginatedComments($postId: String, $page: PageInput) {
  getPaginatedComments(postId: $postId, page: $page) {
    nodes {
      id
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
