query Post {
  posts {
    id
    body
    author
    createdAt
  }
}

mutation CreatePost($input: AddPostInput!) {
  createPost(input: $input) {
    post {
      id
      body
      author
      createdAt
    }
  }
}
