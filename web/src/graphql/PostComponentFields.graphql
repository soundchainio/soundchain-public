fragment PostComponentFields on Post {
  id
  body
  mediaLink
  createdAt
  commentCount
  repostCount
  totalReactions
  topReactions(top: 2)
  myReaction
  profile {
    id
    displayName
    profilePicture
  }
}
