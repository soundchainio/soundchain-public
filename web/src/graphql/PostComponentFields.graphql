fragment PostComponentFields on Post {
  id
  body
  mediaLink
  repostId
  createdAt
  commentCount
  repostCount
  totalReactions
  topReactions(top: 2)
  myReaction
  edited
  profile {
    id
    displayName
    profilePicture
  }
}
