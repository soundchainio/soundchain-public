fragment PostComponentFields on Post {
  id
  body
  mediaLink
  mediaThumbnail
  repostId
  createdAt
  updatedAt
  commentCount
  repostCount
  totalReactions
  topReactions(top: 2)
  myReaction
  deleted
  isGuest
  walletAddress
  profile {
    id
    displayName
    profilePicture
    verified
    teamMember
    userHandle
    badges
  }
  track {
    ...TrackComponentFields
  }
}
