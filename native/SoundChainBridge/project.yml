name: SoundChainBridge
options:
  bundleIdPrefix: io.soundchain
  deploymentTarget:
    iOS: "15.0"
    watchOS: "10.0"
  xcodeVersion: "15.0"

settings:
  MARKETING_VERSION: "1.0.0"
  CURRENT_PROJECT_VERSION: "1"
  DEVELOPMENT_TEAM: ""

targets:
  SoundChainBridge:
    type: application
    platform: iOS
    sources:
      - path: SoundChainBridge
        excludes:
          - "*.xcassets"
      - path: SoundChainBridge/Assets.xcassets
    settings:
      INFOPLIST_FILE: SoundChainBridge/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: io.soundchain.bridge
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
      SWIFT_VERSION: "5.0"
      TARGETED_DEVICE_FAMILY: "1,2"
      CODE_SIGN_STYLE: Automatic
      ENABLE_PREVIEWS: YES
    dependencies:
      - target: SoundChainWatchApp
    entitlements:
      path: SoundChainBridge/SoundChainBridge.entitlements
      properties:
        com.apple.developer.networking.multicast: true

  SoundChainWatchApp:
    type: application
    platform: watchOS
    sources:
      - path: WatchExtension/Sources
      - path: WatchApp
    settings:
      INFOPLIST_FILE: WatchApp/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: io.soundchain.bridge.watchapp
      SWIFT_VERSION: "5.0"
      CODE_SIGN_STYLE: Automatic
      ENABLE_PREVIEWS: YES
      WATCHOS_DEPLOYMENT_TARGET: "10.0"
    dependencies:
      - sdk: WatchConnectivity.framework
      - sdk: UserNotifications.framework
      - sdk: WatchKit.framework
